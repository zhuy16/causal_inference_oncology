# Jupytext configuration
# Each notebook is paired with a .py file (percent format).
# Commit only the .py files — .ipynb files are gitignored.
# To regenerate notebooks from .py files after a fresh clone:
#   jupytext --sync notebooks/*.py

formats = "ipynb,py:percent"
notebook_metadata_filter = "-all"
cell_metadata_filter = "-all"
